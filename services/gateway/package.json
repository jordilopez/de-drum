{
  "name": "de-drum-gateway",
  "version": "1.0.0",
  "description": "API Gateway for de-drum — Fastify proxy between frontend and backend",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "fastify": "^5.3.0",
    "@fastify/cors": "^11.0.0",
    "@fastify/multipart": "^9.0.0",
    "@fastify/rate-limit": "^10.0.0"
  }
}
